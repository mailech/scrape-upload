# Point this at a TEST database first (NOT prod). The tool writes here.
# Standard postgres URL (Neon test branch, local postgres, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Optional: port for the local web UI (default 5070)
PORT=5070
